"""研究员 Prompt - 查询计划生成 & 查询改写"""

# ── 查询计划生成 ──
RESEARCHER_PLAN_PROMPT = """你是一个企业知识库检索专家。你的任务是根据用户的问题，生成多个不同角度的检索查询，以便从向量数据库中获取最相关的文档。

用户问题:
{query}

要求:
1. 生成 {max_queries} 个不同角度的检索查询，每个查询应聚焦于问题的不同方面
2. 查询应使用中文关键词组合，便于向量检索匹配
3. 每个查询应独立成行，不要编号
4. 如果用户问题比较简单，可以只生成 1-2 个查询

请直接输出查询列表，一行一个，不要输出任何其他内容。"""

# ── 查询改写 ──
RESEARCHER_REWRITE_PROMPT = """你是一个企业知识库检索专家。之前的检索没有找到相关文档，你需要改写查询以改善检索结果。

原始查询: {original_query}
之前尝试的查询: {previous_queries}
改写次数: {rewrite_count} / {max_rewrites}

要求:
1. 分析为什么之前的查询没有找到相关文档
2. 使用不同的关键词表述、同义词、或更宽泛/更具体的术语
3. 生成 {max_queries} 个新的检索查询
4. 每个查询独立成行，不要编号

请直接输出改写后的查询列表，一行一个，不要输出任何其他内容。"""